NEXT_PUBLIC_API_URL=/api
POLKASEND_BACKEND_URL=http://localhost:4000

# Default Web2-safe mode. Frontend uses Next API fallbacks and does not depend on backend.
POLKASEND_ENABLE_BACKEND_PROXY=false

# Keep false until contract integration work is resumed.
POLKASEND_ENABLE_CONTRACTS=false
